//For Ex4 I added a functional component called header to components that will display the
//message ”My Header in another component” above the contents component.

function Header(){
    retrurn(
        <div>
            <h2>My Header in another component</h2>
        </div>
    );
}
export default Header;